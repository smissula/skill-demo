class AreaOfCircle {
    public static double calculate(double radius) {
        // double area = (22.0 / 7) * Math.pow(radius, 2);
        double area = Math.PI * Math.pow(radius, 2);
        return area;
    }
}
